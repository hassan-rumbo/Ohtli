# Ohtli Landing Page

Landing page moderna y premium para **Ohtli**, una agencia de desarrollo de software boutique. Inspirada en la fusión de tecnología y arte.

## 🚀 Stack Tecnológico

- **React 19** + **Vite**
- **Tailwind CSS 4** (Estilos y sistema de diseño)
- **Framer Motion** (Animaciones y transiciones)
- **Lucide React** (Íconos)

## 🛠️ Instalación y Ejecución

Sigue estos pasos para levantar el proyecto localmente:

1.  **Clonar el repositorio**

    ```bash
    git clone <tu-repositorio>
    cd teotl-landing
    ```

2.  **Instalar dependencias**

    ```bash
    npm install
    ```

3.  **Iniciar servidor de desarrollo**

    ```bash
    npm run dev
    ```

    El sitio estará disponible en `http://localhost:5173`.

4.  **Construir para producción**
    ```bash
    npm run build
    ```

## 📂 Estructura del Proyecto

- `/src/components`: Componentes reutilizables (Navbar, Footer, Button, Section).
- `/src/sections`: Secciones individuales de la landing (Hero, About, Services, etc.).
- `/src/index.css`: Configuración global de estilos y tema de Tailwind.

## 🎨 Personalización

- **Colores y Fuentes**: Editables en `src/index.css` bajo la directiva `@theme`.
- **Contenido**: Cada sección en `src/sections/` contiene sus propios textos y datos (arrays de objetos).

---

Desarrollado con ❤️ por Teotl Team.
